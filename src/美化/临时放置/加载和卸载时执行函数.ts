/**
 * =====================================================
 * 加载和卸载时执行函数
 * =====================================================
 * 在界面加载完成后和卸载时执行的回调函数
 *
 * 说明：
 * - 必须使用 jQuery 的 $() 而不是原生 DOMContentLoaded
 * - 因为在iframe加载过程中，DOMContentLoaded可能不会被触发
 * - 使用pagehide事件而不是unload事件来检测卸载
 */

$(() => {
  /**
   * 界面加载完成后的回调
   * 显示加载成功的提示消息
   * 使用toastr库显示美化过的提示框
   */
  toastr.success('阿比盖尔美化界面已加载', '提示');
});

$(window).on('pagehide', () => {
  /**
   * 界面卸载时的回调
   * 显示卸载提示（可选，根据需要启用）
   * 可以在此处添加清理资源、取消订阅等操作
   */
  // toastr.info('阿比盖尔美化界面已卸载', '再见');
});
